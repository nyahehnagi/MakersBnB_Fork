<!DOCTYPE html>
<head>
  <link rel="stylesheet" href="/styles.css">
<h1>Maker House a Home</h1>
</head>
  
Available Properties:

<ol>
<% @spaces.each do |space| %>
  <li>
    <%= space.name %>
    <%= space.description %>
    <%= space.price.to_f %>
    <%= space.owner_customer_id %>
  </li>
    <% end %>
</ol>

Choose from the list above which property you would like:
  
<form action='/bookings' method='post'>
<label for='property'>Property:</label>  
<select name='property' id='property'>
  <% @spaces.each do |space| %>
 
  <option value="property_name"> <%= space.name %>'s <%= space.description %> for £<%=space.price.to_f %>   </option>
  
  <% end %>
</select>

<input type='submit' value='Make booking'/>
</form>
